app:
  message:Welcome to config server school service

Spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/school_service_db
    username: postgres
    password: pgs@123!
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
          dialect:org.hibernate.dialect.PostgreSQL81Dialect
    show-sql: true